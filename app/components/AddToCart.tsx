'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("added to cart")}>Cart</button>
    </div>
  )
}

export default AddToCart
